version: 0.2

phases:
  pre_build:
    commands:
      - echo "Installing test dependencies..."
      - pip install pytest
      
  build:
    commands:
      - echo "Running tests..."
      - python -m pytest tests/ || echo "No tests found"

artifacts:
  files:
    - '**/*'
  base-directory: '.'